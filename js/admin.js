// Dữ liệu mẫu để hiển thị cho bảng điều khiển
document.addEventListener("DOMContentLoaded", function() {
    // Cập nhật số lượng người dùng
    document.getElementById("user-count").textContent = 50;

    // Cập nhật số lượng bài viết
    document.getElementById("post-count").textContent = 15;

    // Cập nhật chỉ số AQI trung bình
    document.getElementById("avg-aqi").textContent = 80;
});
